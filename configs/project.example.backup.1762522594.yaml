L1:
  compliance_map: "configs/compliance_map.yaml"
  reports_dir: "reports"
  notes: "Demo compliance mapping across EU AI Act, GDPR, HIPAA, ISO/IEC, SaMD"
  evidence:
    - "docs/intended_use.pdf"
    - "docs/pms_plan.pdf"

L2:
  # Your existing L2 demo settings (stub metrics not required here)
  metrics: {}
  score_hint: 90
  evidence: ["configs/security.yaml"]

L3:
  # Your current L3 settings (keep as-is for your demo data)
  csv_path: "data/fairness_example.csv"
  label_col: "y_true"
  pred_col: "y_pred"   # labels 0/1   (set pred_col to y_prob and use_prob: true if you add probs)
  group_col: "group"
  use_prob: false
  notes: "Balanced demo run"

L4:
  # Your current L4 settings
  csv_path: "data/explain_demo.csv"
  label_col: "y"
  feature_cols: ["f1", "f2", "f3", "f4"]   # optional; inferred if omitted
  test_size: 0.3
  top_k: 1                                  # number of features to "delete"
  notes: "L4 permutation-importance deletion test"

L5:
  # Your existing L5 demo settings (stub metrics still OK for demo)
  metrics: {"logging_coverage": 0.97, "alert_latency_h": 0.8}
  score_hint: 88
  evidence: ["logs/telemetry.csv"]
